# 0.4.1

## Added

- Agent Gateway protocol v2 at `GET /v2/ws`, including multiplexed operation subscriptions, replay, sequence deduplication, gap reporting, lifecycle events, and executor-aware tool routing.

## Compatibility

- The v1 `GET /ws?operationId=...` protocol remains available.
- The Agent Gateway alignment target is `aeaf0abc63134974f803eebf4c1851c5e8f7bf37`.

## Distribution

- Docker images are built for linux/amd64 on a self-hosted Linux runner and published to `ghcr.io/zhouguanyang/lobehub-gateway`.
