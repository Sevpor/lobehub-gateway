# 0.3.3

## Changed

- Sync with upstream `0.3.2`
- Build Docker releases for linux/amd64 on a self-hosted Linux runner
- Publish Docker images to `ghcr.io/zhouguanyang/lobehub-gateway`
- Authenticate GHCR publishing with the `GHCR_USERNAME` and `GHCR_TOKEN` Actions secrets
